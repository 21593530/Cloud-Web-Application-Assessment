export default function SettingsPage() {
  return (
    <section className="panel" aria-labelledby="settings-heading">
      <p className="eyebrow">Settings</p>
      <h2 id="settings-heading">Theme and Preferences</h2>
      <p>
        This page will include light and dark theme controls with persistence so
        teachers keep their chosen interface preferences.
      </p>
      <p>
        Next build step: theme toggle, cookie persistence, and optional layout
        preference controls.
      </p>
    </section>
  );
}

export default function WordlePage() {
  return (
    <section className="panel" aria-labelledby="wordle-heading">
      <p className="eyebrow">Wordle Builder</p>
      <h2 id="wordle-heading">Phoneme Wordle Setup</h2>
      <p>
        This page will let teachers configure a single phoneme-based Wordle word,
        preview gameplay, and generate a downloadable HTML file.
      </p>
      <p>
        Next build step: input controls, phoneme hints, preview panel, and
        Generate button.
      </p>
    </section>
  );
}

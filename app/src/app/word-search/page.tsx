export default function WordSearchPage() {
  return (
    <section className="panel" aria-labelledby="word-search-heading">
      <p className="eyebrow">Word Search Builder</p>
      <h2 id="word-search-heading">Phoneme Word Search Setup</h2>
      <p>
        This page will let teachers enter a small phoneme word list, preview a
        generated word search, and download a standalone playable HTML file.
      </p>
      <p>
        Next build step: word list controls, grid controls, puzzle preview, and
        Generate button.
      </p>
    </section>
  );
}
